services:
  server:
    container_name: server
    image: server
    build:
      context: ./
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "5000:5000"
    volumes:
      - app_log:/app/logs
    restart: unless-stopped


volumes:
  app_log:
